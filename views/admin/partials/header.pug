header.header 
    .container-fluid 
        .row-align-items-center 
            .col-3 
                .inner-logo 
                    a(href="/admin/dashboard") ADMIN
            .col-10 
                div(class="text-right")
                    a(
                        href=`${prefixAdmin}/my-account`
                        class="btn color-button btn-sm mr-2"
                    ) #{user.fullName}
                    a(
                        href=`${prefixAdmin}/auth/logout`
                        class="btn color-button btn-sm"
                    ) Đăng xuất 